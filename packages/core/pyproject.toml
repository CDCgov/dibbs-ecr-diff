[project]
name = "core"
version = "0.1.0"
description = ""
dependencies = [
    "lxml>=6.0.2",
]

[dependency-groups]
dev = [
    "types-lxml>=2026.2.16",
]

[tool.setuptools.package-data]
core = ["configurations/*.json"]
